# Plan

-[x] Rename `AudioEngine` to `RecordingEngine` and keep behavior identical (non-functional rename only). -[x] Extract Scales playback state/scheduling into a new `PlaybackEngine` with a similar public API shape where it makes sense. Keep Scales and Recorder pages as thin adapters that subscribe to engine UI state and send commands. -[x] Hoist both engines to AppShell-level singletons initialized once outside React (module scope), so they are not tied to page mount/unmount or React double-render. -[x] Wire page/view transitions to call engine start/stop/wanted-running methods so default behavior stays exactly as it is today. -[ ] Add opt-in settings (below auto-pause) for `keepRecordingOnAllPages` and `keepPlayingOnAllPages`, with persisted config and settings UI wiring. -[ ] Add/update focused tests for lifecycle invariants (single init, no unmount reset, view switch behavior, settings gating) before functional behavior changes. -[ ] Manually verify Android Chrome flows (Scales -> Recorder -> Scales, background/foreground, About page, permission prompts, battery impact).
